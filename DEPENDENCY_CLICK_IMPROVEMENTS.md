# Улучшение поведения кликов по зависимостям

## Проблема

Ранее все зависимости (независимо от статуса установки) открывались во внешнем браузере, что было неудобно для установленных модов.

## Решение

### 🎯 **Новое поведение:**

1. **Установленная зависимость** → выделение в списке модов (зеленым цветом)
2. **Неустановленная зависимость** → открытие в браузере приложения

### 🔧 **Реализация:**

#### 1. Проверка статуса установки
```python
def _on_dependency_click(self, mod_id: str):
    # Проверяем, установлен ли мод
    installed_mods = self.mod_manager.get_installed_mods(self.current_game.steam_id)
    installed_mod_ids = {m.mod_id for m in installed_mods}
    
    if mod_id in installed_mod_ids:
        # Установленная зависимость - выделяем в списке
        self._select_mod_by_id(mod_id)
    else:
        # Неустановленная зависимость - открываем в браузере
        self._open_mod_in_browser(mod_id)
```

#### 2. Временное выделение зеленым цветом
```python
def _highlight_dependency(self, list_ctrl: wx.ListCtrl, item_index: int):
    # Устанавливаем зеленый фон для элемента
    item = wx.ListViewItem()
    item.SetId(item_index)
    item.SetBackgroundColour(wx.Colour(144, 238, 144))  # Светло-зеленый
    list_ctrl.SetItem(item)
    
    # Убираем выделение через 3 секунды
    wx.CallLater(3000, self._clear_dependency_highlight)
```

#### 3. Обновленные tooltip
- **Установленные**: `"ID: {dep_id}\nУстановлен - кликните для выделения в списке"`
- **Неустановленные**: `"ID: {dep_id}\nНе установлен - кликните для открытия в браузере"`

### 🎨 **Визуальные индикаторы:**

#### Установленные зависимости:
- ✅ **Гиперссылка** (синий цвет, подчеркивание)
- ✅ **Tooltip**: "Установлен - кликните для выделения в списке"
- ✅ **При клике**: выделение в списке + зеленая подсветка на 3 секунды

#### Неустановленные зависимости:
- ❌ **Обычный текст** (серый цвет)
- ❌ **Tooltip**: "Не установлен - кликните для открытия в браузере"  
- ❌ **При клике**: открытие страницы мода в браузере приложения

### 🔄 **Процесс работы:**

1. **Клик на установленную зависимость:**
   ```
   Пользователь кликает → Проверка статуса → Найден в списке → Выделение + зеленая подсветка
   ```

2. **Клик на неустановленную зависимость:**
   ```
   Пользователь кликает → Проверка статуса → Не найден → Открытие в браузере приложения
   ```

### 🛠️ **Технические детали:**

#### Новые переменные:
```python
self.highlighted_item = None      # Индекс выделенного элемента
self.highlighted_list = None      # Список с выделением
```

#### Новые методы:
- `_highlight_dependency()` - временное выделение зеленым
- `_clear_dependency_highlight()` - очистка выделения
- Обновленный `_on_dependency_click()` - умная логика клика

#### Автоматическая очистка:
- Выделение убирается через 3 секунды
- Очистка при закрытии вкладки
- Очистка при новом выделении

### 📋 **Пример использования:**

#### Сценарий 1: Установленная зависимость
```
Мод A зависит от Мода B (установлен)

1. Пользователь открывает детали Мода A
2. Видит зависимость "Мод B" (синяя ссылка)
3. Кликает на "Мод B"
4. Мод B выделяется в списке модов + зеленая подсветка на 3 сек
```

#### Сценарий 2: Неустановленная зависимость  
```
Мод A зависит от Мода C (не установлен)

1. Пользователь открывает детали Мода A
2. Видит зависимость "Мод C (123456)" (серый текст)
3. Кликает на зависимость
4. Открывается страница Мода C в браузере приложения
```

### 🎯 **Преимущества:**

#### ✅ **Удобство использования:**
- Быстрый переход к установленным зависимостям
- Понятное визуальное разделение статусов
- Информативные tooltip

#### ✅ **Логика поведения:**
- Установленные моды → навигация внутри приложения
- Неустановленные моды → внешняя информация
- Автоматическая очистка выделения

#### ✅ **Визуальная обратная связь:**
- Зеленая подсветка показывает результат клика
- Разные стили для разных статусов
- Временное выделение не мешает работе

### 🔮 **Будущие улучшения:**

1. **Настройка времени подсветки** - пользователь может выбрать длительность
2. **Цветовые темы** - выбор цвета подсветки
3. **Групповые действия** - выделение всех зависимостей сразу
4. **История навигации** - возврат к предыдущему выделению

### 🐛 **Обработка крайних случаев:**

- **Мод удален во время работы** → graceful fallback
- **Список модов пуст** → сообщение об отсутствии
- **Несколько кликов** → очистка предыдущего выделения
- **Закрытие вкладки** → автоматическая очистка

## Итог

Теперь зависимости работают интуитивно понятно:
- **Установленные** → быстрый доступ в приложении  
- **Неустановленные** → информация для установки

Это значительно улучшает пользовательский опыт и делает навигацию по зависимостям более логичной и удобной.
