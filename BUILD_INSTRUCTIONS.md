# 🚀 Сборка GameModManager в EXE файл

## 📋 Что нужно для сборки

### 1. Установите зависимости:
```bash
pip install -r requirements.txt
pip install pyinstaller
```

### 2. Способы сборки:

## 🎯 Способ 1: Простой скрипт (Рекомендуется)
```bash
python build_with_pyinstaller.py
```

## 🔧 Способ 2: Через PyInstaller напрямую
```bash
pyinstaller GameModManager.spec
```

## 📦 Способ 3: Быстрая сборка (без spec файла)
```bash
pyinstaller --name=GameModManager --windowed --onefile --clean main.py
```

## 📁 Результат

После сборки исполняемый файл будет находиться в:
```
dist/GameModManager.exe
```

## ⚙️ Оптимизация размера

Для уменьшения размера файла можно использовать UPX:
```bash
# Установите UPX (если еще не установлен)
# Windows: скачать с https://upx.github.io/

# PyInstaller автоматически использует UPX если он доступен
```

## 🐛 Возможные проблемы и решения

### Проблема: "ModuleNotFoundError"
**Решение:** Добавьте недостающий модуль в `hiddenimports` в `GameModManager.spec`

### Проблема: Файл не запускается
**Решение:** Проверьте логи сборки, возможно нужно добавить данные в `datas`

### Проблема: Большой размер файла (>100MB)
**Решение:** 
- Используйте UPX сжатие
- Добавьте больше модулей в `excludes`
- Рассмотрите режим `--onedir` вместо `--onefile`

## 📝 Создание дистрибутива

Для распространения создайте папку с:
```
GameModManager/
├── GameModManager.exe
├── README.md
└── assets/ (если есть)
```

## 🔍 Тестирование

Перед распространением обязательно протестируйте:
1. Запуск на чистой системе (без Python)
2. Все основные функции
3. WebView компонент
4. Работа с файлами

## 📌 Дополнительные настройки

### Добавление иконки:
1. Создайте файл `assets/icon.ico`
2. Раскомментируйте строку в `GameModManager.spec`:
   ```python
   icon='assets/icon.ico'
   ```

### Версия и информация о файле:
Добавьте в `GameModManager.spec`:
```python
version='1.0.0',
description='Game Mod Manager for Steam games',
```

## 🎉 Готово!

Теперь у вас есть автономный EXE файл, который могут запускать другие пользователи без установки Python!
